---
- name: Copy wazuh-docker folder to NFS share
  ansible.legacy.copy:
    src: "{{ base_dir }}/docker/wazuh-docker/"
    dest: "{{ nfs_mount_point }}/wazuh-docker/"
    owner: root
    group: root
    mode: '0755'
